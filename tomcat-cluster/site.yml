---
# This playbook deploys a simple standalone Tomcat 7 server. 

- hosts: tomcat-servers 
  remote_user: root

  roles:
    - selinux
    - tomcat
    - webapp

- hosts: lbservers
  remote_user: root

  roles:
    - selinux
    - haproxy
